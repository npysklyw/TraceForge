# Application connection; /health still does not require a database.
DATABASE_URL=postgresql+psycopg://traceforge:traceforge@localhost:5432/traceforge
# Test suites create and drop only a unique traceforge_test_* schema.
TEST_DATABASE_URL=postgresql+psycopg://traceforge:traceforge@localhost:5432/traceforge
