# Application connection; /health still does not require a database.
BENCHWARDEN_DATABASE_URL=postgresql+psycopg://benchwarden:benchwarden@localhost:5432/benchwarden
# Test suites create and drop only a unique benchwarden_test_* schema.
BENCHWARDEN_TEST_DATABASE_URL=postgresql+psycopg://benchwarden:benchwarden@localhost:5432/benchwarden
